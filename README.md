To setup the project, run the following commands:

npm install
npm run start

To run the tests, use the following command:

npm run test

Examples:

* Get all tasks: GET /api/tasks
* Update task priority: PUT /api/tasks/:id